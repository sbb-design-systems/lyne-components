name: PR Title Linter
on:
  pull_request:
    types: [opened, edited, reopened, unlocked]

permissions: read-all

jobs:
  lint:
    runs-on: ubuntu-slim
    steps:
      - uses: sbb-design-systems/actions/.github/workflows/pr-title-lint.yml@main
