os: linux
language: node_js
jobs:
  include:
  - stage: build and deploy storybook
    script:
    - npm run build
    deploy:
      provider: netlify
      cleanup: false
      dir: "storybook-static/"
      site: $NETLIFY_SITE_ID
      auth: $NETLIFY_AUTH
      edge: true
