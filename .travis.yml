os: linux
language: node_js
# cache:
#   directories:
#   - node_modules
  include:
    - stage: test
    - script: npm run test
    - stage: build
    - script: npm run build
    - stage: publish
    - script:
      - echo "//registry.npmjs.org/:_authToken=${NPM_PUBLISH_TOKEN}" > .npmrc
      - npm publish
